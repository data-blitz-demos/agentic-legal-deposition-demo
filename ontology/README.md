# Ontology Folder

Place one or more legal ontology `.owl` files here for Graph RAG import.

Examples:

- `sasl_legal_ontology.owl`
- `core_legal_terms.owl`

The API container mounts this folder at `/data/ontology`.
